<div class="container">   

    {{#each userPosts}}
    
    <div class="card-header header-post"><a href="/dashboard/edit/{{id}}" title="Edit / Delete">
        <h2>{{title}}</h2>
        <div class="left">{{postDate createdAt}}</div>        
        </a>
    </div>

    {{/each}}

</div>